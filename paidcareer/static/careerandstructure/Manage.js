/** autogenerated **/
Ext._define('paidcareer.careerandstructure.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getPMCareerMoreStructureGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('paidcareer.careerandstructure.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'PMCareerMoreStructure'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getPMCareerMoreStructureGrid(),
                ]
            }
        );

        paidcareer.careerandstructure.Manage.superclass.constructor.call(this, cfg);
    }
});

