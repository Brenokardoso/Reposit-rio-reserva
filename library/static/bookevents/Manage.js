/** autogenerated **/
Ext._define('library.bookevents.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getBookEventsGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('library.bookevents.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'BookEvents'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getBookEventsGrid(),
                ]
            }
        );

        library.bookevents.Manage.superclass.constructor.call(this, cfg);
    }
});

