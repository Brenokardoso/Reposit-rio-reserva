/** autogenerated **/
Ext._define('library.book.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getBookGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('library.book.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Book'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getBookGrid(),
                ]
            }
        );

        library.book.Manage.superclass.constructor.call(this, cfg);
    }
});

