/** autogenerated **/
Ext._define('library.category.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getCategoryGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('library.category.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Category'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getCategoryGrid(),
                ]
            }
        );

        library.category.Manage.superclass.constructor.call(this, cfg);
    }
});

