/** autogenerated **/
Ext._define('library.author.Manage', {
    extend: 'toolkit.widget.TabPanel',

    getAuthorGrid: function() {
        if(!this._grid) {
            this._grid = Ext._create('library.author.Grid', {
                region: 'center',
                gridAutoLoad: false
            });
        }

        return this._grid;
    },

    constructor: function(cfg) {
        cfg = core.nullValue(cfg, {});

        Ext.applyIf(
            cfg,
            {
                title: 'Author'
            }
        );

        Ext.apply(
            cfg,
            {
                layout: 'border',
                items: [
                    this.getAuthorGrid(),
                ]
            }
        );

        library.author.Manage.superclass.constructor.call(this, cfg);
    }
});

